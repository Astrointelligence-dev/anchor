# Corte da 0.2.0 — sessão 13

**Data:** 2026-09-08 · **Branch:** dev (`bb2d5cb`, limpa, = origin/dev) · **Entrada:** [[repo-anchor]] § "Próximo: corte da 0.2.0"; mecânica em `2026-08-31-release-engineering.md`.

## Estado verificado na abertura (tudo por execução, não por leitura)

| Fato | Valor |
|---|---|
| PyPI `astro-anchor` | só `0.1.1`; `project_urls` apontam para `artcgranja/*` (owner antigo) |
| Pages antiga `artcgranja.github.io/anchor` | **404, sem redirect** — links de "Docs" no README/pyproject/docs quebrados hoje |
| Pages nova `astrointelligence-dev.github.io/anchor` | 200 (branch `gh-pages`, deploy por `docs.yml` em push na main) |
| `origin/main` vs `dev` | main tem `990092a` (URLs da org no README, jul/2026) que a dev **não tem**; `git merge-tree` → **CONFLITO em README.md** |
| `main` local | 1 atrás de `origin/main` |
| CI na dev | **vermelho desde que passou a rodar na dev** (sessão 8): job Lint falha com os 150 erros de ruff que toda sessão chamou de "baseline"; `test` tem `needs: lint` → **a suíte nunca rodou no GitHub para nenhum commit da v0.2** |
| `release.yml` | não roda ruff/mypy; roda a suíte em 3.11/3.12/3.13 → check tag==versão → `uv build` → publish OIDC (env `pypi`) |
| Suíte local | 3.11 (venv do projeto) e 3.13 (env limpo `--all-extras`): **3140 verdes, 10 skipped**, fora `tests/live` (1 "falha" era o Rich quebrando linha por causa do meu `--basetemp` longo — passa com tmp padrão) |
| `tests/live` local | `test_memory_llm_live` roda o golden set no `claude_cli` (minutos); no CI é pulado (sem `claude`) |
| Wheel (`uv build`) | 228 arquivos, os 20 subpacotes + `py.typed`; sem lixo |
| `mkdocs build --strict` | passa (aviso do MkDocs 2.0 vs Material, informativo) |
| ruff `src/ tests/` | 150: 66 auto (+12 unsafe); manuais: E501 19, N806 18 (tests/llm gemini+litellm), A004/A001 12 (`TimeoutError` próprio sombreia o builtin), E402 11 (tests/test_memory), C901 6 (providers/agent), B017 3, RUF059 3, F841 2, F811 2 (**um teste morto** em `test_tools.py:38` redefinido em :360), F821 1 (`agent/models.py:45` — `ToolSchema` só importado dentro do método; inofensivo em runtime pelo `from __future__ import annotations`), SIM/RUF/PT 6 |
| mypy `src/` | 140 erros em 36 arquivos (arg-type 29, attr-defined 25, union-attr 18, no-any-return 16, misc 15, unused-ignore 7, import-untyped 4…) |
| Versão | `pyproject` 0.1.1; `__version__` via `importlib.metadata`; `tests/test_exports.py:361` fixa `"0.1.1"` |
| CHANGELOG | `[Unreleased]` completo (Breaking/Added/Changed/Fixed/Removed, linhas 8–161); `docs/docs/changelog.md` é cópia manual **defasada** (sem 0.1.1, sem nada da v0.2); mkdocs tem `pymdownx.snippets` |
| GitHub | main sem proteção; merge commit permitido; nenhuma Release criada até hoje; env `pypi` não visível pela API (o workflow o declara; GitHub cria no 1º run) |
| `sync-docs.yml` | despacha para `artcgranja/astro-docs` (outro repo) — fora do escopo |

## Decisões do Arthur (antes de executar)

- [x] **D1 — CI vermelho.** ✅ **(a)** (a) ruff → 0 agora + mypy como passo informativo (`continue-on-error`) e mypy → 0 na 1ª sessão pós-release · (b) ruff → 0 e mypy → 0 agora (adia o corte) · (c) cortar com CI vermelho (o `release.yml` não olha lint). **Recomendado: (a)** — o que importa pro corte é a matriz de testes rodar de verdade no GitHub, e 140 correções de tipo às vésperas é risco sem ganho pro usuário.
- [x] **D2 — `TimeoutError` (A004/A001 ×12).** ✅ **(b) renomear para `LLMTimeoutError`** (a) manter o nome, suprimir o lint, follow-up · (b) renomear para `LLMTimeoutError` agora (breaking; a 0.2.0 é o momento barato). **Recomendado: (a)** para não crescer o corte; (b) se quiser fechar a porta agora.
- [x] **D3 — Review global do diff (156 commits) antes do corte?** ✅ **(a) direto** (a) ir direto — a sessão 8 fez xhigh em `d50dd3c..dev` e #3/#4/#5 tiveram ritual próprio · (b) `/code-review` do acumulado. **Recomendado: (a).**
- [x] **D4 — Autonomia.** ✅ **(a)** (a) executo A→C, empurro pra dev conforme cada fase fecha (o CI verde é a verificação da B), **paro antes do PR e antes da tag** · (b) paro em cada fase · (c) vai até o fim (PR + tag) sem parar.

Default sem objeção: merge com **merge commit** (histórico bisectável, como nas sessões anteriores); tag criada por `gh release create v0.2.0 --target main --notes-file` (tag + GitHub Release com a seção do CHANGELOG; o push da tag dispara o `release.yml`).

## Fases (uma por commit)

### A — Sincronizar com `origin/main` e canonizar URLs
- [x] `git merge origin/main` na dev, README resolvido = conteúdo da dev + URLs da org
- [x] Mesma troca em `pyproject.toml` `[project.urls]` (4), `docs/mkdocs.yml` (`site_url`, `repo_url`, `repo_name`, `magiclink.user`, footer — 5), `docs/docs/index.md` (2), `docs/docs/contributing.md` (2)
- [x] `git remote set-url origin` → `Astrointelligence-dev/anchor` (só config local)
- [x] Verificar: `grep artcgranja` = 0 fora de `docs/plans|research|site` e `sync-docs.yml`; `git merge-tree dev origin/main` limpo; `mkdocs build --strict`

### B — CI honesto
- [x] `ruff check --fix` (66) + revisar os 12 unsafe + manuais (~72): quebras E501; renomes N806 nos 2 testes; E402/noqa; `# noqa: C901` nos 6 defs (follow-up de refatoração); D2; B017 → exceção específica/`match=`; RUF059/F841; **F811: renomear o teste morto e ver se ele passa** (nunca rodou); F821 → import sob `TYPE_CHECKING`
- [x] mypy conforme D1
- [x] Verificar: `ruff check src/ tests/` = 0 ✅; suíte 3.11 verde ✅ (3141); push ✅ → **CI verde na dev (lint + 3 jobs de teste)** ✅ run 34263990062: 3144 verdes em CPython 3.11.16 / 3.12.14 / 3.13.15 — primeira execução real da suíte v0.2 no GitHub (o run anterior expôs a matriz fictícia e o combine do coverage, ver Desvios)

### C — Bump + CHANGELOG
- [x] `pyproject` 0.1.1 → 0.2.0; `uv lock`; `test_exports` → `"0.2.0"`
- [x] CHANGELOG: `## [Unreleased]` vazio + `## [0.2.0] - 2026-09-08` com o conteúdo atual
- [x] `docs/docs/changelog.md` → inclui `CHANGELOG.md` por snippet (`base_path: [!relative $config_dir]`) — acaba o drift na raiz
- [x] Verificar: `__version__` = 0.2.0 ✅; `uv build` gera `0.2.0` ✅; `mkdocs build --strict` ✅ (changelog com 0.2.0 e 0.1.1 via snippet); suíte ✅ 3141; push ✅; CI verde ✅ (mesmo run 34263990062)

### D — PR dev → main *(para antes, conforme D4)* — **⏸ aguardando OK do Arthur** (corpo do PR pronto)
- [ ] `gh pr create --base main --head dev`; CI verde no PR; `gh pr merge --merge`
- [ ] Verificar: árvore de main == dev; CI verde na main; `docs.yml` publica o site novo (curl 200 + changelog com 0.2.0)

### E — Tag + publish *(para antes, conforme D4 — irreversível: versão no PyPI não se reenvia)*
- [ ] `gh release create v0.2.0 --target main --title v0.2.0 --notes-file <seção 0.2.0>`
- [ ] Verificar: `release.yml` verde (3 test jobs → build → publish); PyPI JSON lista `0.2.0` com URLs da org; venv limpo `pip install astro-anchor==0.2.0` + `import anchor, anchor.llm, anchor.graph, anchor.memory` + `anchor.__version__`

### F — Fechamento
- [ ] Obsidian: repo note (linha do roadmap "corte 0.2.0 ✅", Session History, Next Steps = pós-publish), daily
- [ ] Review section aqui: desvios, lições, follow-ups

## Follow-ups (candidatos, decidir no fechamento)
- mypy → 0 (sessão própria) se D1 = (a); refatorar os 6 C901; renomear `TimeoutError` se D2 = (a)
- Pós-publish (decisão da sessão 8): cookbook + migração astro-skills/tui/context
- `tests/test_cli` sensível à largura do tmp path (Rich quebra a mensagem); `test_memory_llm_live` roda o golden set inteiro quando há `claude` local
- `sync-docs.yml` aponta para `artcgranja/astro-docs`
- Follow-ups da #5 (2 chamadas/turno fora do `UsageLimits`; TTL expirado no grafo até o GC; 4 casos estritos do golden set) e da sessão 8 (memória síncrona no caminho async, `_pool_entry`/`_EVENT_SINK`, `expose_tool` sem schema…)

## Desvios encontrados na execução (primeiro run real da suíte no GitHub)

- **A matriz do CI era fictícia**: `.python-version` pina 3.11 e `uv sync` obedecia a ele — os jobs "3.12" e "3.13" (ci.yml **e** release.yml) rodavam CPython 3.11.16 (log do run 34262744747). Fix: `UV_PYTHON=${{ matrix.python-version }}` por job (`819f5b8`).
- **pytest-cov quebrava no combine** ("Can't combine statement coverage data with branch data") com a suíte inteira verde (3144). Causa: sem `--cov-config` explícito o pytest-cov exporta `COV_CORE_CONFIG=":"` aos subprocessos — `resources.py` roda scripts de skill com `sys.executable` e `cwd` no diretório da skill, sem `pyproject` — e cria o `combining_cov` com `.coveragerc` inexistente; esses medem statements enquanto o processo principal lê `branch = true`. Localmente a corrida termina depois do combine (só sobram `.coverage.*` órfãos, `has_arcs: False`); no runner chega antes. Fix: `--cov-config=pyproject.toml` (`819f5b8`). Não é regressão do release: o `release.yml` roda `pytest -q` sem cov.
- O primeiro `hoist()` dos imports concatenados falhou por docstring de uma linha; e o `set -e` não segurou a cadeia após um heredoc Python falhar — script gravado em arquivo e cadeia com `&&` explícito resolveram.
- `--unsafe-fixes` apagou a atribuição de `verbatim_items` (F841) em vez de expor o assert que faltava; reintroduzido como lista + `assert verbatim_items` (passa). Também deixou um `Exception("auth failed")` solto — removido.

## Review
_(preenchido no fechamento)_
